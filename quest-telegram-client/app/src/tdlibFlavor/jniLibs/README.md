Place official TDLib native libraries here by ABI for the `tdlib` flavor, for example:

- `arm64-v8a/libtdjni.so`

Keep fake builds independent from these binaries.
Do not use unofficial prebuilt TDLib binaries.
